# The Vite dev server proxies /api to the backend (see vite.config.js),
# so no VITE_API_URL is required for local development.
# For production, set this to your deployed backend URL and update
# src/api/client.js to use it as the baseURL instead of "/api".
VITE_API_URL=http://localhost:5000/api
